<!DOCTYPE html>
<html lang="en">
  <head>
      <meta charset="UTF-8">
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" 
            rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" 
            crossorigin="anonymous">
      <title>Contact</title>
  </head>
  <body>
    <%- include('Nav') %>
    <div class="container mt-5">
      <h1 class="mb-2 text-info">Liste Contact</h1>
        <table class="table">
          <thead>
            <tr>
              <th scope="col">Id</th>
              <th scope="col">Nom</th>
              <th scope="col">Prénom</th>
              <th scope="col">Email</th>
              <!-- <th scope="col">password</th> -->
              <th scope="col">Message</th>
              <th scope="col">Mettre à jour</th>
              <th scope="col">Supprimer</th>
            </tr>
          </thead>
          <tbody>
            <!-- 
              Boucle Foreach pour la récupération de chaque ligne {contact} :
              data.foreach((contact)=>{
                contact.nom 
                contact.prenom 
                contact.email
                contact.message 
              })
            -->
            <% data.forEach((contact)=>{%>
            <tr>
              <th scope="row"><%= contact.id %></th>
              <td><%= contact.nom %></td>
              <td><%= contact.prenom %></td>
              <td><%= contact.email %></td>
              <!-- <td><%= //contact.password %></td> -->
              <td><%= contact.message %></td>
              <td><a href="http://localhost:5000/contact/edit/<%= contact.id %>" class="btn btn-warning">Modifier</a></td>
              <td>
            <!-- 
              SUPPRIMER : (Ne pas oublier d'installer la dépendance Method-override)
              1 - L'url action inclut l'id suivi de la synthaxe: ?_method=DELETE
                  action="chemin/contact.id?_method=DELETE" 
                  "contact.id" dépend du nom de l'objet dans le foreach
              2 - ajouter input type "hidden" avec name="_method" et value="DELETE"
                  <input type="hidden" name="_method" value="DELETE">
            -->
              <form method="post" action="/contact/delete/<%= contact.id%>?_method=DELETE">
                <input type="hidden" name="_method" value="DELETE">
                  <input type="submit" class="btn btn-danger" name="delete" value="Delete">
              </form>
            </td>
            </tr>
            <% })%>
          </tbody>
        </table>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" 
            integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" 
            crossorigin="anonymous">
    </script>
  </body>
</html>